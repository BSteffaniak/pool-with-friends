# cargo-deny configuration for PWMTF.

[advisories]
version = 2
yanked  = "deny"

[licenses]
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MIT",
    "MPL-2.0",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.8
version = 2

[bans]
multiple-versions = "allow"
wildcards         = "deny"

[sources]
allow-git        = []
unknown-git      = "deny"
unknown-registry = "deny"
